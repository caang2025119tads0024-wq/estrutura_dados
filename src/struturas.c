#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
struct pessoas{   // Aqui definimos a estrutura 
         int idade;
         float peso;
         char nome[50];
    };

// Aqui definimos o tipo de variavel 
typedef struct pessoa tipo_pessoa; 


int main(){
    
    setlocale(LC_ALL, "portuguese");
    

    str
    
}